@model HobbyMood.Models.ViewModels.ExperienceNew

@{
    ViewData["Title"] = "New Experience";
}

<h1>New Experience</h1>

<form method="POST" asp-action="Add">
    <div class="form-group">
        <label for="ExperienceName"><strong>Experience Name:</strong></label>
        <input type="text" name="ExperienceName" class="form-control" required />
    </div>

    <div class="form-group">
        <label for="HobbyId"><strong>Hobby:</strong></label>
        <select name="HobbyId" class="form-control">
            @foreach (var hobby in Model.HobbyOptions)
            {
                <option value="@hobby.HobbyId">@hobby.HobbyName</option>
            }
        </select>
    </div>

    <div class="form-group">
        <label for="ExperienceDate"><strong>Date:</strong></label>
        <input type="date" name="ExperienceDate" class="form-control" required />
    </div>

    <div class="form-group">
        <label for="ExperienceLocation"><strong>Location:</strong></label>
        <input type="text" name="ExperienceLocation" class="form-control" />
    </div>

    <div class="form-group">
        <label for="DurationinHours"><strong>Duration (hours):</strong></label>
        <input type="number" step="0.1" name="DurationinHours" class="form-control" />
    </div>

    <div class="form-group">
        <label for="ExperienceCost"><strong>Cost ($):</strong></label>
        <input type="number" step="0.01" name="ExperienceCost" class="form-control" />
    </div>

    <h4>Add Moods</h4>
    <div id="moodContainer">
        <div class="mood-entry">
            <select name="moodId" class="form-control">
                @foreach (var mood in Model.MoodOptions)
                {
                    <option value="@mood.MoodId">@mood.MoodName</option>
                }
            </select>
            <label for="BeforeIntensity">Intensity Before:</label>
            <input type="number" name="BeforeIntensity" min="0" max="10" style="width: 50px;" />

            <label for="AfterIntensity">Intensity After:</label>
            <input type="number" name="AfterIntensity" min="0" max="10" style="width: 50px;" />
        </div>
    </div>

    <br />

    <input type="submit" value="Create Experience" class="btn btn-primary" />
    <a asp-action="List" class="btn btn-secondary">Cancel</a>
</form>
